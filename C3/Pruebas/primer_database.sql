personasCREATE TABLE Personas (ID INT AUTO_INCREMENT PRIMARY KEY, nombre VARCHAR(40), fecha DATE);
CREATE TABLE Telefonos (numero VARCHAR(12), id_personas INT NOT NULL REFERENCES Personas (ID) ON DELETE CASCADE ON UPDATE CASCADE);