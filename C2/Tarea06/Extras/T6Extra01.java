import javax.swing.JOptionPane;

public class T6Extra01 {
    public static void main(String[] args) {
        Falta comprobar los dosprimeros caracteres, demomento solo uno.
        
        // String str1 = JOptionPane.showInputDialog("Introduzca la primera palabra");
        // String str2 = JOptionPane.showInputDialog("Introduzca la segunda palabra");
        // char[] caracteres1 = str1.toCharArray();
        // char[] caracteres2 = str2.toCharArray();
        // String car1 = String.valueOf(caracteres1[0]);
        // String car2 = String.valueOf(caracteres2[0]);
        // boolean soniguales = false;
        // if (car1.equals(car2)) {
        //     soniguales = true;
        // }

        // if (soniguales) {
        //     JOptionPane.showMessageDialog(null,
        //             "Exacto, estas palabras empiezan por " + car1 + "\n - " + str1 + "\n - " + str2);
        // } else {
        //     JOptionPane.showMessageDialog(null, "Que pena, estas palabras no epieza por la misma letra:\n - " + str1
        //             + " ==> " + car1 + "\n - " + str2 + " ==> " + car2);
        // }
    }
}
